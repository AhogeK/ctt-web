# Progress: ctt-web

## Milestone Overview

| Milestone                         | Status      | Target Version |
| --------------------------------- | ----------- | -------------- |
| Project Scaffold                  | ✅ Complete | 0.1.0          |
| Router Architecture               | ✅ Complete | 0.2.0-beta.2   |
| Layout System                     | ✅ Complete | 0.2.0-beta.3   |
| Auth Module                       | ✅ Complete | 0.3.0-beta.1   |
| Lazy Loading + Chunk Optimization | ✅ Complete | 0.4.0-beta.2   |
| GitHub OAuth                      | ✅ Complete | 0.8.3          |
| hCaptcha Integration              | ✅ Complete | 0.7.6          |
| Terms Acceptance                  | ✅ Complete | 0.7.0          |
| Dashboard                         | ⏳ Pending  | 0.9.0          |
| Device Management                 | ⏳ Pending  | 0.9.0          |
| Leaderboard                       | ⏳ Pending  | 1.0.0          |
| Set Password                      | ✅ Complete | 0.10.0         |
| CSRF Protection                   | ✅ Complete | 0.10.2         |
| Settings                          | ⏳ Pending  | 1.0.0          |
| i18n (zh/en)                      | ⏳ Pending  | 1.0.0          |
| E2E Test Coverage                 | ⏳ Pending  | 1.0.0          |
| Production Deploy                 | ⏳ Pending  | 1.0.0          |

## Completed (Recent)

- [x] **v0.19.0 (2026-08-31)** Dashboard D1: stats API contract layer — stats.schema.ts (7 response schemas + DistributionType enum, z.infer only), lib/api/stats.ts (7 functions, timezoneOffset auto from browser per request, optional deviceId filter), useStats.ts composables (STATS_QUERY_KEYS factory isolating endpoint+params, staleTime 60s summary/heatmap/achievements per server cache, 30s rest), stats.test.ts 14 cases. Contract read from ctt-server StatsController source (R13). 1142/1142 (68 files), type-check/lint green. 0.18.3 → 0.19.0 (MINOR).
- [x] **v0.18.3 (2026-08-30)** Device revoked state (user-reported bug: revoke left the list unchanged) — backend v0.50.0 adds `revokedAt` to DeviceResponse (set on revoke, cleared on re-registration); frontend DeviceSchema + revokedAt, DeviceListView renders destructive "Revoked" badge + hides Revoke button. Tests updated across schema/API/component/E2E (helper DELETE now SETS revokedAt, revoke.spec asserts Revoked). Real-backend verified: null → timestamp on revoke, card flips to Revoked, button gone, record kept. 1128/1128 unit + 9/9 E2E, type-check/build/lint green. 0.18.2 → 0.18.3 (PATCH).

- [x] **v0.18.2 (2026-08-30)** Device Management O (E2E coverage) — e2e/devices suite (fixtures/helpers + list/revoke/errors specs, 9/9 chromium); DeviceListView data-testid=device-list/device-card added for stable locators. Gotchas: CI=true forces preview mode (4173) — run E2E via `env -u CI` to reuse dev server; page.reload() after setup bounces to login with mock tokens — seed failing routes before first navigation instead. 1126/1126 unit + 9/9 E2E, type-check/build/lint green. 0.18.1 → 0.18.2 (PATCH).

- [x] **v0.18.1 (2026-08-30)** Device Management O (unit + component coverage) — new devices.test.ts (7) + device.schema.test.ts (10) + useDevices.test.ts (6); consistency fix: useRevokeDevice return shape unified to `{ mutation }` (was raw useMutation, diverged from useRevokeApiKey) with DeviceListView.vue + its test updated; DeviceListView.test.ts component suite (11, from v0.18.0) adapted. 1126/1126 (67 files), type-check/build/lint green. 0.18.0 → 0.18.1 (PATCH). E2E pending next.

- [x] **v0.18.0 (2026-08-29)** Device Management N: error-code mapping + edge polish — COMMON_002 remapped to not-found semantics (real 429 is RATE_LIMIT_001, verified); DEVICE_001 added; shared time utils extracted to src/lib/utils/time.ts (formatRelativeTime/formatDateTime) used by DeviceListView + ApiKeysView; DeviceListView first-load Skeleton ≥300ms anti-flicker, per-row Revoke aria-label, absolute-time hover title, empty-state "Install the JetBrains plugin" link; ApiKeysView inline formatters removed + explicit (name: string) typing for dialog success-description arrows (vue-tsc TS7006 after helper extraction). Tests… 1102/1102 (64 files, +26), type-check/build/lint green, browser-verified. 0.17.3 → 0.18.0 (develop).

- [x] **SKILL_GRAPH.md sync (2026-08-24)** — rebuilt the skills index against measured reality: ~/.agents 371 + config 88 + project .agents 38 + project .claude 35 = 465 deduped (was 393/487). Fixed 52 ghost refs: removed 22 uninstalled superpowers skills (+ flow-section references replaced with idea-refine/tdd/debugging/hunt), fixed 19 unprefixed gstack names to gstack-* (careful→gstack-careful etc.), added 6 opencode built-ins (debugging/visual-qa/review-work/remove-ai-slops/init-deep/customize-opencode), removed 5 stale entries (iso-13485-certification, obsidian-vault, duplicate finish-a-development-branch). Verified: 0 skills missing from graph, 23 sections intact, tables unbroken. AI content — committed on develop only, NOT cherry-picked to master.

- [x] **Deps update (2026-08-24)** — `vp update -L` upgraded 19 deps (lucide 1.34, tanstack-query 5.102.2, pinia 4.0.3, reka-ui 2.10.3, vue-i18n 11.4.9, commitlint/faker/jest-dom/tsconfig/jsdom30/node-types/vitest/oxlint/vite-plus-core0.3.0 etc.). TS7 attempt: vue-tsc@3.3.11 peer `>=5.0.0` suggested compatibility but type-check failed (`ERR_PACKAGE_PATH_NOT_EXPORTED`) → reverted to exact 6.0.3 (reinforced red line). Fixed 2 pre-existing e2e lint warnings (no-conditional-in-test) per user's no-excuse rule. All green: peers/type-check/build/unit 1078/lint 0 error 0 warning. Version unchanged 0.17.3 (deps-only).

- [x] **v0.17.3** SetPasswordDialog show-password toggles — user reported password fields in Set/Change dialog had no visibility toggle while Login/Register forms do. Fixed by mirroring the LoginForm/RegisterForm pattern: three toggles (showCurrentPassword/showNewPassword/showConfirmPassword) with Eye/EyeOff buttons in relative wrappers (pr-10, absolute right-2.5, tabindex=-1, aria-labels). +2 tests (new password toggle; current password toggle in change mode). 1078/1078 unit + vue-tsc/lint 0 error. Version 0.17.2 → 0.17.3.

- [x] **v0.17.2** SetPasswordDialog server-error field mapping — user reported "Current password is incorrect." showed in a bottom banner: (1) banner conditional mount/unmount shifted layout (form jump), (2) style inconsistent with per-field FormMessage validation errors. Fixed: removed errorMessage ref + bottom div; server business errors now map via form.setFieldError onto the matching field (USER_014 → currentPassword; PASSWORD_SAME_AS_OLD/COMMON_003/USER_015 → newPassword) so they render in the same FormMessage slot as validation errors — no layout jump, consistent style. Unknown errors: composable toast only. Error-handling tests rewritten (4 tests assert setFieldError mapping), useForm mock gained setFieldError, ref import removed. 1076/1076 unit + vue-tsc/lint 0 error. Version 0.17.1 → 0.17.2.

- [x] **v0.17.1** USER_014 resource-401 no-logout fix — user changed password with wrong current password on real backend v0.44.0: backend correctly returned 401 USER_014, but frontend logged the user out. Root cause: `handle401Error` whitelist only excluded AUTH_010 (plus refreshable AUTH_002/003 and TERMINAL_AUTH_CODES); USER_014 (business check on authenticated user) fell through to the session-failure branch (clear token + UNAUTHORIZED_EVENT → redirect login). Fixed: USER_014 added to the resource-level 401 no-logout branch alongside AUTH_010; comment documents both codes + the resource-vs-session 401 contract. +2 tests (flat + wrapped formats). 1075/1075 unit + vue-tsc/lint 0 error. Version 0.17.0 → 0.17.1.

- [x] **v0.17.0** Change Password feature (dual-mode dialog) + Set Password bug fixes — user reported: (a) Set Password dialog lacked password strength meter; (b) dialog never closed after success; (c) button flips to "Change Password" but dialog/backend can't change (dead end 409 USER_015). Root causes: (a) PasswordStrengthMeter simply absent in SetPasswordDialog; (b) AccountSection kept its own local ref while useSetPassword's module-level shared isDialogOpen did the closing on mutation success — composable's write never reached parent binding; (c) backend only has POST /users/me/password/set (first-time only). Decision: implement REAL change flow. Frontend: user.ts changePassword() (both fields base64 — transport convention, backend doesn't decode), useSetPassword.changePasswordMutation (distinct toast, closes shared ref, invalidates), SetPasswordDialog dual-mode via hasPassword prop (dynamic title/desc/button, conditional current-password field, USER_014/PASSWORD_SAME_AS_OLD inline errors), AccountSection passes :has-password. Review fixes: subagent's `as any` removed + schema made reactive (computed → validationSchema, vee-validate MaybeRef) so current-password validation follows hasPassword after profile refresh; form.test.ts mock synced with changePasswordMutation. Backend change endpoint = requirement text to user (R3). Tests: +10 → 1073/1073 unit + vue-tsc/lint 0 error. Version 0.16.20 → 0.17.0 (new feature → MINOR; 0.16.20 never committed).

- [x] **v0.16.19** 5.2 rate-limit (429 RATE_LIMIT_001) verification + E2E form-preservation — real backend: creates #1-10 201, #11 429 RATE_LIMIT_001; body has NO retryAfter field and no Retry-After header (frontend countdown `getRetryAfterSeconds` is forward-compatible: header → body instant → static toast). Closed E2E coverage gap for 5.2b: on 429 the dialog stays open and the typed name is preserved (previously only toast visibility was asserted). api-keys E2E 22/22, unit 1061/1061. Version 0.16.18 → 0.16.19.

- [x] **v0.16.18** Error-code split AUTH_014→AUTH_024 (API key limit) — backend reused AUTH_014 for both API-key 20-limit AND token unique-constraint conflicts ("Token creation failed" message is correct for tokens, non-descriptive for keys). Backend split: new AUTH_024("Maximum active API keys reached", 409), AUTH_014 retained for token cases. Frontend synced 8 files: api-error.ts mapping (text unchanged), CreateApiKeyDialog constant + JSDoc, useApiKeys.ts/api-keys.ts JSDoc, 3 unit mocks, e2e fixtures (AUTH_014_BODY→AUTH_024_BODY) + errors.spec. No remaining AUTH_014 refs in ctt-web (token case is backend/other-client only). 1061/1061 unit + 22/22 api-keys E2E + lint/type green. Version 0.16.17 → 0.16.18.

- [x] **v0.16.17** 4.5.2 delete-constraint defense verification + E2E double-click test — verified against real backend (2 bootstrapped accounts): ACTIVE delete → 409 AUTH_023 ("Active API keys must be revoked before they can be deleted" — doc's "Only revoked API keys..." is stale), nonexistent/repeat/BOLA delete → 401 AUTH_010, owner's key intact; frontend: ACTIVE→Revoke only, REVOKED+EXPIRED→Delete (intended drift since v0.16.7), triple pending guard (JS early-return + :disabled + close-block) + unit double-click test. Closed the E2E gap: new delete.spec.ts test holds the delete response open (manual route release), dispatches 2nd click via dispatchEvent (click() blocks on actionability; accessible name switches to "Deleting..."), asserts exactly 1 request. api-keys E2E 22/22, unit 1061/1061. Reinstalled Playwright chromium (cache was missing; npm refused EBADDEVENGINES, used pnpm). Version 0.16.16 → 0.16.17.

- [x] **v0.16.16** Component Render Error fix (scopes unregister crash) — toggling Custom → Recommended unmounted the scopes FormField, and vee-validate 4 unregisters fields on unmount, leaving `form.values.scopes` undefined → submit button `scopes.length` threw. Fixed with `keepValuesOnUnmount: true` on useForm. +1 real-integration regression test (form.test.ts, real vee-validate: toggle → scopes intact → submit works); fixed latent mock bug in form.test.ts checkbox template (`as` cast invalid in runtime-compiled templates). EmailChangeDialog password field audited — guarded by `|| ''`, unaffected. 1061/1061 unit + vue-tsc/lint green. Version 0.16.15 → 0.16.16.

- [x] **v0.16.15** Custom date field: click the mm/dd/yyyy text area opens the native picker — the custom expiration `<input type="date">` only responded to the calendar indicator icon; `handleDateFieldClick` now calls `showPicker()` (user-gesture gated, try/catch for already-open/unsupported); `handleDateFieldMouseDown` suppresses native segment text selection WITHOUT real focus (iterated: `input.focus()` makes Chrome auto-select first segment "mm"; dropping focus lost the focus ring — CSS cannot hide segment highlight, pixel-verified headed Chrome, UA-internal). Final: fake focus styling via `dateFieldActive` ref (border-primary + ring), cleared on change / outside click (document listener in CAPTURE phase — reka-ui DialogContent stops bubble mousedown) / dialog close. Zero new deps, native display preserved. +3 tests (text-click calls showPicker; open-picker no-crash; mousedown prevents default + fake focus + clears on change). 1060/1060 unit + vue-tsc/lint green; verified in headed Chrome (headless does not render datetime-edit pseudo-elements): no real focus, ArrowUp no-op after Escape, fake ring on click + clears outside. Version 0.16.14 → 0.16.15.

- [x] **v0.16.14** Custom-mode scope descriptions (GitHub PAT style) — always-visible one-line purpose text for READ/WRITE/SYNC/ADMIN at checkbox time (hover tooltips rejected: vanish on touch devices; scope purpose is decision-critical). SCOPE_DESCRIPTIONS map aligned with ctt-server ApiKeyScope semantics; label layout checkbox + two-line text (name + muted description); dual-axis code review PASS (2 omo sub-agents, 0 findings). Tests 1057/1057 unit + 21/21 api-keys E2E + vue-tsc/lint green. Version 0.16.13 → 0.16.14.

- [x] **v0.16.6** ConfirmApiKeyActionDialog description alignment fix — mobile revoke dialog's description first word ("After") was offset from the left edge and rendering varied across phone widths. Two causes: (1) multi-line `{{ }}` interpolation condensed into a leading space (Vue whitespace:condense); (2) AlertDialogHeader default `text-center sm:text-left` centered the header on mobile. Fixed via single-line interpolation + `AlertDialogHeader class="text-left"`. +1 regression test. Browser-verified at 7 widths (320–1024px): no leading space, text-align left everywhere. 1050/1050 unit + 38/38 chromium E2E.

- [x] **v0.16.5** Mobile sidebar final UX — brand icon dropped from mobile entirely (user: "放在里面看起来怪怪的"); mobile sheet now auto-closes on navigation (`handleNavigate` → `setOpenMobile(false)` on all 4 menu items; desktop no-op). Mobile header chrome: none (neither header branch matches). AppSidebar tests: shared setOpenMobile spy via vi.hoisted box; +2 nav-close tests. Browser-verified: sheet opens with no icon, removed from DOM after menu click. 1049/1049 unit + 38/38 chromium E2E.

- [x] **v0.16.4** Mobile sidebar sheet animation + brand icon fix — SheetContent.vue mounted slide-in/out animation classes unconditionally (outside side ternaries): side='left' accumulated slide-in-from-left/right/bottom → enter transforms are separate CSS vars, so the drawer flew in from the bottom-right. Canonical shadcn form restored (each side ternary carries its own position + animations). AppSidebar mobile header restored as left-aligned PluginIcon (v0.16.3 removed it entirely; user expects the brand). Browser-verified at 375px: only slide-in-from-left, icon present left-aligned, no trigger in sheet. 1047/1047 unit + 38/38 chromium E2E. [Brand-icon decision superseded by v0.16.5 — mobile header chrome removed entirely.]

- [x] **v0.16.3** Mobile sidebar header fix — `AppSidebar` header `v-else` branch (desktop-collapsed centered icon cell) leaked into the mobile Sheet: opening the drawer showed a pointless centered PluginIcon. Changed to `v-else-if="!isMobile"` (mobile header chrome omitted; Sheet owns close, AppHeader owns trigger). AppSidebar.test.ts useSidebar mock made mutable via vi.hoisted box + computed; +1 mobile test. Browser-verified at 375px. 1047/1047 unit + 38/39 chromium E2E (only pre-existing vue.spec root-url failure). [Superseded by v0.16.4 for the mobile-header decision — brand icon now kept on mobile; then v0.16.5 removed it again.]

- [x] **v0.16.2** RawKeyDialog reka-ui a11y warning fix — manual `id`/aria attrs on DialogTitle/DialogDescription/DialogContent orphaned reka's generated ids → dev-mode `useWarning` fired `requires a DialogTitle` + `Missing Description` on every create→raw dialog switch. Removed the manual ids/aria (kept `role="alertdialog"` + hard-to-dismiss); reka now wires aria-labelledby/aria-describedby automatically like every other dialog. New real-reka unit test + E2E a11y-warnings spec. Post-review polish: tooltips for Created/Last Used/Expires columns unified to readable formatDateTime (was raw ISO on two columns); test teardown hardening (console.warn restore via afterEach, E2E console listener off). 1046/1046 unit (62 files) + 20/20 API-key E2E green.

- [x] **v0.16.1** hCaptcha CSP sentry fix — CaptchaWidget `:sentry="false"` stops api.js loading with sentry=true, eliminating the inline-script CSP violation (`prepare.js` console error on login page). Backend ctt-server v0.41.1 separately fixed quoted CSP hosts. 1041/1041 unit + 19/19 E2E green.

- [x] **v0.16.0** API Key permanent delete — REVOKED keys deletable (backed by ctt-server v0.41.0 `DELETE /{id}/delete`); ACTIVE/EXPIRED no delete path (revoke-first, 409 AUTH_023 server-enforced). Frontend: deleteApiKey API + useDeleteApiKey + **shared ConfirmApiKeyActionDialog** (revoke & delete unified — 95%-duplicate dialogs merged per review, not deferred to a 3rd) + REVOKED-row Delete buttons (table + mobile cards); AUTH_023 mapping; README sync. Tests: 1041/1041 unit + 19/19 E2E (incl. chained revoke→delete, AUTH_023/BOLA toasts, mobile card flow).

- [x] **v0.15.5** API-key schema NON_NULL fix (real-integration, blocking create) — ctt-server `jackson.default-property-inclusion: non_null` omits null fields → frontend `.nullable()` threw "expected string, received undefined" on create (fresh key lastUsedAt/revokedAt absent; backend v0.40.2 also fixed createdAt omission server-side). Fixed ApiKeySchema + blast-swept device.schema/oauth-account.schema to `.nullable().default(null)` (user.schema precedent). +3 regression tests; 1029/1029 green. Verified against real backend v0.40.2: create/list/get/revoke/BOLA all pass, keyPrefix now 14-char cttak_ prefixed.
- [x] **v0.15.4** Sidebar layout regression fix — sidebar rendered on the RIGHT + collapse broken. Root cause: `@vue-ignore` + circular `SidebarProps` import (`'.'` barrel) made Rolldown SFC compiler silently drop the whole `defineProps` declaration → `side`/`collapsible`/`variant` defaults never compiled. Fixed: standalone `props.ts` breaks the cycle, `@vue-ignore` removed. Verified browser: left side, fixed, 224px, collapse works; 1026/1026 + type-check + lint + build green.

- [x] **v0.15.1** API Key E2E Suite + Bug Fixes — e2e/api-keys/ 5 specs (list/create/revoke/errors/rawkey-dialog) 13/13 pass; **fixed 2 pre-existing blocking bugs** (CreateApiKeyDialog Name via defineField+passive-Input never synced → feature unusable; **SetPasswordDialog + EmailChangeDialog same defineField binding — OAuth password-set/email-change forms were broken too**, both switched to FormField pattern + Input passive removed); playwright config pnpm webServer + 1080p viewport; CreateApiKeyDialog.test.ts form stubs. Dual-axis review fixes: +3 createApiKey unit tests, errors.spec dead-code + inline-GET dedup via route.fallback(), helpers.ts stale comment, +5 useApiKeys composable tests (query/mutation), +1 RawKeyDialog aria-labelledby assertion, +1 E2E countdown-toast test (retryAfter body). Real-form integration tests (CreateApiKeyDialog.form.test.ts + SetPasswordDialog.form.test.ts + EmailChangeDialog.form.test.ts, +6) now guard the binding class across all three dialogs. Round-2: removed dead defineField mock (CreateApiKeyDialog.test.ts), dead helpers re-export, as-never escapes. **Lighthouse a11y 91/100** (acceptance #4 met; lighthouse@13.4.1 devDep; fixed light `--muted-foreground` #8a8f98→#62666d per DESIGN.md). **Peer warning fixed** (zod 4 vs @vee-validate/zod ^3.24.0 declaration lag → `peerDependencyRules.allowedVersions.zod: ^4.4.3` in pnpm-workspace.yaml). 1026/1026 unit + 13/13 E2E. Revoke double-click idempotency: already tested (stale Spec finding).

- [x] **v0.15.0** API Key Responsive Card View — deferred <768px item pulled back into scope (user decision); `ApiKeysView` table wrapped `hidden md:block` + new `md:hidden` card view (per-key cards: name/status/prefix/scopes/metadata, ACTIVE-only Revoke); DeviceListView precedent, no duplicate Create button. Tests +3 → 1006/1006 (58 files); type-check + build + lint clean. Round review: data-testid hardening (api-key-table/cards/card), hover unified /30, card Revoke click test + skeleton-card-hidden test (+2 → 1008/1008).

- [x] **v0.14.0** API Key Edge Polish (J) — `getRetryAfterSeconds` dual-source helper (Retry-After header / retryAfter body ISO; defensive null) + CreateApiKeyDialog 429 countdown toast (backend sends no timing info today — static fallback); first-load Skeleton ≥300ms anti-flicker (gated on no-cached-data); a11y (sr-only table caption, per-row Revoke aria-label); AppHeader Settings item → `/settings/api-keys`. Tests +18 → 1003/1003 (58 files); type-check + build + lint clean. Deviations: no dayjs (R12, existing formatter equivalent), no breadcrumb (no project convention), no new ApiKeysErrorState/SettingsLayout (inline error state complete, stale spec paths).

- [x] **v0.13.0** API Key Revoke Flow (M3) — `revokeApiKey()` (DELETE 204 idempotent, no envelope parse) + `useRevokeApiKey()` mutation (invalidates `['api-keys']`); AUTH_010 interceptor fix in `instance.ts` (BOLA 401 no longer logs user out — error propagates for generic message); `RevokeApiKeyDialog.vue` (AlertDialog destructive confirmation, key name+prefix, pending guard, success toast/error keeps dialog open, explicit Cancel default focus via `@open-auto-focus` preventDefault); `src/components/ui/alert-dialog/` reka-ui wrappers (new UI primitives); `ApiKeysView.vue` Actions column Revoke button enabled for ACTIVE rows + `selectedKeyForRevoke` cleanup on close. Tests: api-keys.test.ts (+7), instance.test.ts (+3 AUTH_010), useApiKeys.test.ts (+5), RevokeApiKeyDialog.test.ts (+8), ApiKeysView.test.ts (+5). 985/985 pass (58 files); type-check + build + lint clean.
- [x] **Notion H 节完成总结（2026-08-02）** — Notion「ctt-web 开发计划」H 节（创建流程 + RawKey 一次性展示）更新为完成态：全部 checkbox 勾选、交付物 ✅ 已完成、验收标准 ✅、完成记录小节；修正 4 处计划与实现不符的描述（组件路径/API 文件/invalidate key/expiresAt 写法）；G/I 节字节级未变。
- [x] **v0.12.0** API Key Create Flow (M2) — CreateApiKeyDialog (vee-validate form, scope mode toggle, expiration presets + custom date, 409 AUTH_014 inline banner), RawKeyDialog one-time display (overlay/Esc/X blocked, copy-gated close button, three-tier clipboard fallback, alertdialog a11y), `createApiKey()` API + `useCreateApiKey` mutation, `useCopyToClipboard` composable, `expiresAt` future-time Zod refine, AUTH_014/AUTH_010 error mappings. 5 new files, 957/957 tests pass (+29 after post-review toast-path test).
- [x] **v0.11.0** API Key Management (M1) + Build Fixes — Implemented API Keys list page at `/settings/api-keys` with GitHub PAT-style table, loading skeleton, error/empty states, status badges, scope chips, relative time. Added `ApiKeySchema`/`ApiKeysPayloadSchema` to `lib/schemas/`, `listApiKeys()` to `lib/api/`, `useApiKeys` composable. Updated sidebar with API Keys link. Downgraded TypeScript 7.0.2 → 6.0.3 (Vue toolchain incompatible with TS7's removed programmatic APIs). Fixed Rolldown SFC compilation with `script.fs` option. Fixed `formatRelativeTime` future-date bug. 8 files changed, 928/928 tests pass.
- [x] **v0.10.13** Remove dead MSW infrastructure from E2E tests — Deleted `e2e/mocks/browser.ts` (lazy Proxy that could never work in Playwright's Node context), `e2e/global-setup.ts` (no-op globalSetup), `public/mockServiceWorker.js` (generated file, no consumer); removed `globalSetup` from `playwright.config.ts`; converted `e2e/mocks/handlers/auth.ts` from MSW handlers to pure API contract reference (typed response constants, no MSW imports); removed `msw` devDependency + `msw.workerDirectory` from `package.json`; removed `allowBuilds.msw` from `pnpm-workspace.yaml`. Architectural decision: `page.route()` is Playwright's first-class E2E mocking API; MSW is for unit/integration tests. 6 files changed, 18/18 auth specs pass; vue-tsc + `tsc --noEmit -p e2e/tsconfig.json` clean.
- [x] **v0.10.12** E2E MSW wiring + auth spec refactor — Threaded shared fixtures through every auth spec and aligned spec data shapes with the MSW handlers; added `e2e/global-setup.ts` (Playwright `globalSetup`, no-op body in Node) and `public/mockServiceWorker.js` (generated via `pnpm exec msw init public/ --save`); `e2e/mocks/browser.ts` rewritten as a lazy `Proxy<SetupWorkerApi>` that defers `setupWorker()` until first property access (avoids `Invariant Violation` when imported from Node); refactored `login.spec.ts`/`logout.spec.ts`/`protected-routes.spec.ts` to use `e2e/utils/auth-helpers.ts` + `e2e/fixtures/auth.ts` (replaces 150+ lines of inline `page.route()` mock data with shared `okEnvelope` / `STORAGE_KEYS` / `TEST_*` constants); added `e2e/auth/guest-guard.spec.ts` + 4 extra tests (429 rate-limit toast, network-drop toast, `termsExpired` TermsDialog, guest-guard redirect). MSW handlers exist as infrastructure but specs still use `page.route()` via helpers — runtime interception is a follow-up. 18 files changed, 18/18 auth specs pass (chromium, 8.9s); vue-tsc + `tsc --noEmit -p e2e/tsconfig.json` + `vp lint e2e/` clean.
- [x] **v0.10.11** MSW (Mock Service Worker) integration for E2E auth mocking — Installed `msw@2.14.6`; created `e2e/mocks/handlers/auth.ts` (9 endpoints + `/users/me` with `RestApiResponse<T>` envelopes matching Zod schemas), `e2e/mocks/browser.ts` (`setupWorker` singleton), `e2e/fixtures/auth.ts` (canonical credentials + response shapes). Added `dom` lib to `e2e/tsconfig.json`. 4 files changed, vue-tsc + tsc clean.
- [x] **v0.10.10** OAuth sessionStorage redirect + test coverage — Added sessionStorage storage for redirect target before GitHub OAuth redirect; added 15 tests for OAuthCallbackView (happy path, missing tokens, termsExpired, safe/unsafe redirect, URL cleanup); added 10 tests for OAuthErrorView (error code mapping, button navigation, fallback messages). 4 files changed, 928/928 tests pass.
- [x] **v0.10.9** hasPassword integration + code review fixes — Integrated backend `hasPassword` field from API; deprecated `usePasswordDetection` composable; fixed SetPasswordDialog @success regression; added schema type-rejection test; fixed JSDoc field order. 8 files changed, 903/903 tests pass.
- [x] **v0.10.8** Password button label: Set Password → Change Password — button now always visible; label dynamically changes based on `hasPassword` state (null/false → "Set Password", true → "Change Password"). 1 file changed.
- [x] **v0.10.7** Tab title unified to "Code Time Tracker" — browser tab title suffix changed from `- CTT` to `- Code Time Tracker` for brand consistency. `guard.ts` + `guard.test.ts` updated. 2 files changed.
- [x] **v0.10.6** Favicon replaced with PluginIcon SVG — Browser tab icon now uses the Code Time Tracker plugin icon (SVG) instead of the default Vite favicon. `public/favicon.svg` created from `PluginIcon.vue` SVG; `index.html` updated to reference SVG favicon + title changed to "Code Time Tracker". 2 files changed.
- [x] **v0.10.5** Bug fix: remove auto-call of `/api/v1/users/me/password/set` on mount — `usePasswordDetection` had `onMounted` that called `setPassword('')` automatically. Removed auto-call, changed `hasPassword` initial to `null` (tri-state: null/unknown, false/no-password, true/has-password), `isChecking` starts `false`. Detection now lazy via `recheck()` called on "Set Password" button click. 4 files changed, 897/897 tests pass.
- [x] **v0.10.5** Appearance submenu: state-adaptive subtitle color (Round 5) — Round 4's `!text-muted-foreground` was still unreadable on the strong purple accent bg (muted-foreground = dark gray, low contrast on purple). Fixed with Tailwind `group-` variants: subtitle now `text-muted-foreground` in normal state + `group-focus:!text-accent-foreground/80` + `group-data-[state=open]:!text-accent-foreground/80` in highlighted states. SubTrigger gets `class="group"` to enable the selectors. Tests unchanged; 897/897 pass; type-check clean.
- [x] **v0.10.5** Appearance submenu: vertical two-line trigger (Round 4 — SUPERSEDED by Round 5) — Perplexity-style subtitle layout: title "Appearance" + subtitle "System (Light)" below as muted secondary text. Layout correct but static muted-foreground color was unreadable on purple accent bg; Round 5 replaces with state-adaptive coloring.
- [x] **v0.10.5** Appearance submenu: show current theme on trigger (Round 3 — SUPERSEDED by Round 4) — added inline right-aligned current-theme label to SubTrigger. Rejected: cramped on right side + subtitle re-colored on highlight due to specificity loss. Computed + 4-variant tests kept (reused by Round 4).
- [x] **v0.10.5** `lucide-vue-next` → `@lucide/vue` migration — package deprecated upstream. API identical (same icon names, same default-import). 25 source files + 4 test files updated via sed; `package.json` dep + `pnpm-lock.yaml` regenerated. User explicitly approved (R7 dependency change). 895/895 tests pass (pre-Round 3 baseline).
- [x] **v0.10.5** Appearance submenu in avatar dropdown — Round 2 of v0.10.5: replaced standalone ThemeToggle with Perplexity-style `Appearance` submenu inside the avatar dropdown (Light/Dark/System radio group with Circle indicator). Round 1's standalone button was rejected ("有点丑"). Type-check clean (vue-tsc exit 0); vitest runner pre-existing pnpm 11 / `builtin:vite-wasm-fallback` breakage (same as v0.10.4).
- [x] **v0.10.5** ThemeToggle in AppHeader — **SUPERSEDED by Round 2 submenu** — Round 1 added standalone ThemeToggle button to AppHeader (left of avatar). Rejected as visually unpolished; ThemeToggle component kept in tree for AuthLayout use.
- [x] **v0.10.4** Sidebar Icon & Collapse Enhancement — JetBrains plugin icon in sidebar header; `collapsible="icon"` mode with icon swap on hover; Perplexity-style compact layout (sidebar 14rem, menu text 15px, icon 18px); mobile sidebar trigger restored in AppHeader for Sheet overlay accessibility; removed duplicate plugin-icon.svg; SVG cleanup (aria-label, remove hardcoded dimensions); test fixture emailChangePending field added; type-check clean (0 errors)
- [x] **v0.10.2** CSRF Protection — XSRF-TOKEN cookie reading, X-XSRF-TOKEN header injection for state-changing requests, 403 CSRF error handling (toast + page reload); 815/815 tests pass (12 interceptor + 10 cookie parsing + 8 error handling)
- [x] **v0.10.1** CSP Meta Tag — `index.html` defense-in-depth CSP fallback (same-origin + hCaptcha + inline styles + data URIs + clickjacking protection); HTML-only, no code changes; 785/785 tests pass
- [x] **v0.10.0** Set Password Feature — Backend integration (ctt-server Set Password API): `POST /api/v1/users/me/password/set`; Zod schemas, composable (useSetPassword), SetPasswordDialog component (password form with validation), AccountSection updated with Set Password button (shown only for OAuth users); error code USER_015 (password already set); 785/785 tests pass (8 API + 12 composable + 14 component)

- [x] **v0.9.0** Email Change Feature — Backend integration (ctt-server v0.31.2): 5 API endpoints, Zod schemas, composables (useEmailChange, useEmailStatus), AccountSection component (email/verification/display name/registration time), EmailChangeDialog (dynamic password field on USER_013), EmailVerificationBanner (resend with 60s cooldown), `/auth/change-email` route, error codes USER_009/010/011/013/014, auth store `createdAt` field; 751/751 tests pass (37 API + 16 composable + 57 component + ChangeEmailView)

- [x] **v0.8.44** User Profile API + AppHeader Display — `GET /api/v1/users/me` (ctt-server v0.30.0 + v0.30.1 lastLoginAt fix); `lastLoginAt` schema `.nullable().default(null)` for missing-field resilience; `fetchUserProfile()` wired into main.ts (conditional on auth) + login/loginWithOAuth (immediate); Promise lock dedup; 620/620 tests pass

- [x] UserAvatar in Header (v0.8.43): `src/lib/utils/avatar.ts` (stringToHue/stringToAvatarColor/getInitials) + `src/components/app/UserAvatar.vue` (36px circle, hash-derived HSL color, deterministic per user) integrated into `AppHeader.vue` via Tooltip+DropdownMenu. Logout moved into dropdown menu item. 14 new tests, 589/589 pass.

- [x] Sidebar Branding & Copyright (v0.8.42): `AppSidebar.vue` header CTT → "Code Time Tracker"; Logout button → "© 2026 AhogeK" copyright line. `AppSidebar.test.ts` removed 8 Logout tests, added 2 new tests. 575/575 tests pass.

- [x] OAuth Account Unbind Flow (v0.8.41): Closes ctt-server PR-B DELETE endpoint. `unbindOAuthAccount(provider)` in api; state-aware Connect/Disconnect button in ProfileView with Dialog confirmation; `getOAuthUnbindErrorMessage` for AUTH_017 (not linked) + AUTH_018 (last method). approximately 32 new tests (incl. 6 defensive cleanups: redundant @click removal, extractErrorCode helper, COMMON_001 mapping), 580/580 pass.
- [x] OAuth Account Binding Flow (v0.8.40): Closes ctt-server PR-A BIND endpoint. `getGitHubAuthorizeUrl` adds `action` param ('login' | 'bind'); ProfileView handles OAuth callback via `?linked=github` query (success toast + refetch, error toast via 8-code mapping + URL cleanup). 17 new tests, 548/548 pass.
- [x] OAuth Account Binding Status (v0.8.39): Closes `GET /api/v1/auth/oauth/accounts`. New schema/api/view files; `ProfileView` now drives binding state from the backend (loading / error / connected / disconnected) with TanStack Query caching + window-focus refetch. 531/531 tests pass.
- [x] Dependency Update (v0.8.38): Updated indirect dependencies (electron-to-chromium 1.5.379→1.5.380, js-yaml 4.2.0→4.3.0). All 495 tests pass.
- [x] Dependency Update (v0.8.37): Updated reka-ui 2.10.0→2.10.1, vite-plugin-vue-devtools 8.1.3→8.1.4. All 495 tests pass.
- [x] Dependency Update (v0.8.36): Updated vue 3.6.0-beta.16→3.6.0-beta.17, @playwright/test 1.61.0→1.61.1. All 495 tests pass.
- [x] Dependency Update (v0.8.35): Updated @tanstack/vue-query 5.101.0→5.101.1, @commitlint/cli 21.0.2→21.1.0, @commitlint/config-conventional 21.0.2→21.1.0. All 495 tests pass.
- [x] Dependency Update (v0.8.34): Updated @vue/eslint-config-typescript 14.8.0→14.9.0. All 495 tests pass.
- [x] Dependency Update (v0.8.33): Updated lint-staged 17.0.7→17.0.8. All 495 tests pass.
- [x] Dependency Update (v0.8.32): Updated reka-ui 2.9.10→2.10.0. All 495 tests pass.
- [x] Dependency Update (v0.8.31): Updated @types/node 25.9.3→25.9.4. All 495 tests pass.
- [x] Dependency Update (v0.8.30): Updated vue-i18n 11.4.5→11.4.6, @faker-js/faker 10.4.0→10.5.0. All 495 tests pass.
- [x] GitHub Button Loading & NProgress Spinner Fix (v0.8.29)
- [x] AppLayout Router Fix (v0.8.28)
- [x] Captcha Widget UX Fix (v0.8.26)
- [x] Captcha Validation Fix (v0.8.25)
- [x] GitHub Button Hover Fix (v0.8.24)
- [x] GitHub OAuth Frontend (v0.8.3)
- [x] hCaptcha Integration (v0.7.6)
- [x] Terms Acceptance (v0.7.0)
- [x] TermsDialog scrollbar/theme/readOnly fixes (v0.7.1–v0.7.2)
- [x] Accessibility fixes (v0.6.9)
- [x] Console warnings elimination (v0.6.8)
- [x] ResetPasswordView + TypeScript fixes (v0.6.6–v0.6.7)
- [x] Terms acceptance test coverage (v0.6.4)
- [x] UI polish: form spacing, cursor, compact layout (v0.5.75–v0.5.83)
- [x] Password validation sync (v0.5.40)
- [x] Auth initialization & guest guard (v0.5.35–v0.5.38)
- [x] Ghost button system (v0.5.43–v0.5.57)
- [x] Dependency updates (v0.8.9–v0.8.23) — routine updates via `vp update`

## Completed (Early)

- [x] Technology stack selection & project scaffold
- [x] Router architecture (feature-based, type-safe meta, guards)
- [x] Layout system (AuthLayout + AppLayout, mobile responsive)
- [x] Auth module (JWT login, token management, router guards)
- [x] Lazy loading & chunk optimization
- [x] Error handling (ErrorBoundary, 404View, Sonner toast)
- [x] API layer (ofetch instance, JWT interceptor, global error handling)
- [x] Vite+ migration (unified toolchain via `vp` CLI)

## Backlog

- [ ] Auth: Token refresh + expiry handling
- [ ] Dashboard: Coding heatmap (ECharts)
- [ ] Dashboard: Language distribution pie chart
- [ ] Dashboard: Hourly patterns chart
- [ ] Devices: Device list + last seen
- [ ] Devices: API key create / revoke
- [ ] Leaderboard: Redis ZSet ranking display
- [ ] Settings: Language switch (zh-CN / en-US)
- [ ] CI: GitHub Actions (lint + test + build)
