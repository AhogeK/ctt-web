### 光标抖动（`Send reset link`）✅ **已解决 —— 由回撤我自己的改动结束** ✓（2026-09-24）

- **用户确认** ✓：**回撤我的代码后闪烁消失** ✓ ⇒ **根因是我改出来的 bug** ✗，**原代码本来没有这个缺陷** ✓✓。
- **我改过又撤回的两条（都不是原代码的缺陷 ✗ 均已废弃 ✓）**：
  1. `disabled:cursor-not-allowed` 系列改动（去掉 `disabled:pointer-events-none` + 给 hover 加 `enabled:` 守护 ✗）⇒ **引入新的 hover 闪烁** ✗（调用点未加守护 ✓ 禁用态反而会亮 ✓）。
  2. 移除悬停缩放（1.02 ✗）⇒ 实测命中框确实随悬停态跳 3.8px ✓（`448/384` ⟷ `444.2/391.7` ✓）**但**它**不是**用户现象的根因 ✗ ⇒ 也回撤 ✓。
- **红线（防复燃 ✗）**：**不得**再把这两条当成"待修缺陷"重新实施 ✗ —— 用户已实测"回撤即正常" ✓；若将来真要在 `ui/button` 上动 cursor / pointer-events / scale，**必须先复现原始症状并拿到前后对照数据** ✓（不能凭"这看着像缺陷"就改 ✗）。
- **探测事实（仍然有效 ✓）**：我的干净 Chromium 复现不了 ✓（网格/移动/多宽度/各状态/突变计数全清 ✓）；relay 每跳慢 ⇒ 单次 `run` ≤30s 只能放少量点 ✗；relay 接管要求**唯一目标串** ⇒ 先 `browser-use js` 设 `document.title` ✓。
- **误判记账** ✗（两处，均作废 ✓）：① 归因"扩展"（仅"存在注入宿主"✓ 无因果 ✗）；② 拿**窗口标题**当证据 ✗ —— 那是**正在讨论该 bug 的对话页面**的标题 ✓（AX/窗口文本是页面内容 ✓）。

### 亮色主题 hover 对比度缺陷（2026-09-24，v0.51.2 ✓ 已修）

- **用户报告**：亮色下"所有有 hover 变化的按钮都看不了" ✗ + 指出灰底"跟卡片底几乎一个颜色" ✗。
- **实测定位（两主题 ✓ canvas 归一化 ✓）**：① **主按钮** hover 底变**更亮的紫** `#828fff` ✗ 白字 ⇒ **2.87** ✗（**9 个调用点自带 `hover:bg-accent-hover`** 盖过变体 ✗ ← "所有按钮"的真来源 ✓）；② **outline** hover 底 `#7170ff` ✗ 白字 ⇒ **3.84** ✗；③ **outline + 调用点 `hover:bg-secondary`**（`#f3f4f5` ✗ **与卡片白底对比仅 1.10** ✗ 用户判断正确 ✓）⇒ **1.10** ✗。
- **修法（零新令牌 ✓ 全部由既有令牌派生 ✓）**：变体 `default`/`outline` 的 hover 底改为 `color-mix(in oklab, var(--primary|--accent) 85%, black)` ✓；**15 个文件**里的 9 处调用点统一到同一条工具类 ✓（产物去重后 **1 条** ✓）；`AccountSection`/`ProfileView` **删掉 `hover:bg-secondary`** ✗ ⇒ 交给变体深紫 ✓。
- **修后实测** ✓：主按钮 **6.70** ✓ · 提交按钮 **6.70** ✓ · outline **5.59**（亮）/ **19.93**（暗）✓ · 设置页组合 **5.59**（亮）/ **18.73**（暗）✓ · 单测 **1450/1450** ✓。
- **唯一未达标且有意保留** ✗：**ghost** 亮色 hover 文字 `#7170ff` on `#f7f8f8` = **3.61** ✗ —— 该项用户已明确认可（"发光效果非常不错" ✓）并按 **R30 冻结** ✓ ⇒ **不擅自改** ✗；如需提升可只调**文字色**（不动发光 ✓）⇒ 归用户决定 ✓。
- **教训（两条 ✓）**：① 变体的 hover 底**必须与卡片底明显分化** ✗（`#f3f4f5` vs `#ffffff` = 1.10 不可接受 ✓）；② **调用点自带的 hover 类会静默盖过变体** ✗ ⇒ 改按钮观感时必须**同时扫调用点** ✓（否则只改一半 ✓ 正如"所有按钮"的成因 ✓）。

### 规则变更：外部 AI 咨询改由用户投递（2026-09-24 ✓ AGENTS R21）

- **用户裁定** ✗：「下次你如果要问AI，就停下来，让我来问！避免你使用不够聪明的模型」⇒ **R21 重写** ✓：
  需要外部高级 AI 意见时，我**只写 brief 并停下** ✓（背景一句话 · 硬约束**给数字** · 已失败的路**引用用户原话** · 请对方给的 4 问 ✓），**投递归用户** ✗；
  **仍允许**只读事实检索 ✓（WebSearch / read 直读 / grep.app 等 ✓）。
- **落地三处** ✓：`AGENTS.md` R21 ✓ · `problem-report` 技能（§两种提问模式加"投递归用户"硬边界 ✓ + 反模式一条 ✓）✓ · `ai-chat-browser` 顶部加 **GATE：must be user-initiated** ✓（它不在 `~/.agents/` ✓ 实际在 `~/.config/opencode/skills/` ✓）。
- **brief 范例已固化** ✓：用户点名表扬了那次自发提问 ✓ ⇒ 原文 + "它为什么生效"（第一句关掉选项式回答 ✓ 约束全是数字 ✓ 失败路附原话 ✓ 末尾 4 问 ✓）写入
  `~/.agents/skills/problem-report/assets/report-template.md` ✓（并注明**不进报告正文** ✗ 投递是用户的事 ✓）。

### 主按钮 hover 改为「外轮廓 + 阴影层级」（2026-09-24，v0.51.3 ✓ 实测通过）

- **定稿做法** ✓：主按钮 hover **填充与文字完全不动** ✗ ⇒ 只动 `box-shadow` ✓ ——
  静止 `0 1px 2px rgba(0,0,0,0.3)` ✓ ⇒ 悬停 `0 0 0 1px rgba(94,106,210,0.6), 0 4px 12px rgba(0,0,0,0.35)` ✓（1px 品牌外轮廓 + 沉降阴影 ✓）· `box-shadow` 180ms `cubic-bezier(.2,.8,.2,1)` ✓（reduce 下关闭 ✓）。
  **实测**（preview 与 dev、亮/暗 ✓）：静止/悬停两串阴影如上 ✓ · 填充 `rgb(94,106,210)` 不变 ✓ · 白字不变 ⇒ 对比度 **4.70** ✓。
- **单一来源** ✓：**9 处调用点的 `hover:bg-accent-hover` / `hover:scale-[1.02]` / 旧 hover 阴影一并撤除** ✗ ⇒ 状态样式只由变体与 `main.css` 的 `[data-variant='default']` 规则决定 ✓；配方已按要求**先登记 `DESIGN.md` §6/§10** ✓ 再落成 `--btn-shadow-rest/hover` ✓。
- **两条更正（我之前说过头 ✗）**：① "任何提亮都不达标" ✗ —— 按 WCAG 公式 `#5e6ad2`+白字 = **4.6999** ✓，混白到 **≈2.35%** 才触 4.5 ✓（2% ⇒ `rgb(97,109,211)` ≈ 4.53 ✓）；但 2% 不足以作为设计方案 ✓；② "填充不动＝零无障碍风险" ✗ —— 还须查**非文本对比**：相邻面辨识 · 键盘 `focus-visible` · 外晕被裁切 · **强制颜色模式**（`box-shadow` 会变 `none` ⇒ 需保留可见边框/焦点环 ✓）。
- **两个工程陷阱（血泪 ✓）**：① Tailwind v4 **静默丢弃** `hover:shadow-[0_4px_12px_rgba(0,0,0,0.35)]` ✗（`hover:ring-1` 却编得出 ✓）⇒ 复杂阴影改走 **`main.css` 纯 CSS + `data-variant` 选择器** ✓；② **`data-variant` 只在调用点显式传了 `variant` 时才渲染** ✗（9 处都没传 ⇒ 属性为 `null` ⇒ 按它写的 CSS **永远不匹配** ✓）⇒ 修在组件一处 ✓：`Button.vue` 改为 `:data-variant="variant ?? 'default'"` ✓（让属性反映**生效后**的变体 ✓）。

### hover 口径定稿（2026-09-24，用户三条裁定 ✓）

- **`outline` 家族 = 不用底色** ✗ ⇒ hover 只做 **边框 + 阴影**（`hover:shadow-sm` ✓ + 调用点给边框色 ✓）；**实心按钮才允许变底** ✓（主按钮变**更深**的紫 ✓ 由 `--primary` 派生 ✓）。
- **填充必须与语义同族** ✗：破坏性按钮（Disconnect GitHub ✓）走**红边框** ✓ —— 我先给它紫色填充被否 ✗（"跟红边框非常不配" ✓）。
- **暗色不要用"抬面深灰"分家** ✗（"深灰作为底色非常丑" ✓）⇒ 面层**全部还原** ✓，**分家靠边框** ✓（`--border/--input/--sidebar-border` = `rgba(255,255,255,0.12)` ✓ 保留 ✓）。
- **实测量级**（真机 ✓ 两主题 ✓）：主按钮 hover 白字 = **6.70** ✓（原 2.87 ✗）；outline = 底色不变 + 边框/阴影变化 ✓；ghost 未动 ✓（发光与文字色不变 ✓）。
- **主按钮 hover：用户裁定"变亮 + 白字"** ✓（2026-09-24 二次裁定）：文字**不得翻色** ✗ —— 翻色被判「从按钮 A 变成按钮 B，毫无设计关联性」✗ ⇒ **回退到原样**（`hover:bg-accent-hover` ✓ 白字不动 ✓）；其实测 **2.87** ✗ 属**已知并被用户接受** ✓（R30：不要再"修"它 ✗）。**数学事实** ✓：白字要 ≥4.5 ⇒ 底色亮度**不得高于静止态** ✗ （`--primary` 混白 10%/7%/4% ⇒ 3.90 / 4.15 / 4.37 全部 ✗）⇒ "更亮 + 白字" 与 AA **互斥** ✓，二者只能取一 ✓。
- **实现禁忌（记账 ✗）**：调用点自带的 hover 类会**静默压过**变体 ⇒ 改按钮观感**必须同时扫调用点** ✓；给"实心按钮"写在**任意 hover 背景**里都要先问一句"这个色与它的边框/语义同族吗" ✓。

### `.omp/` markdown 统筹（2026-09-24 ✓）

- **根目录只留目录级文件** ✓：`README.md` · `ai-planning-method.md`；11 个业务产物按类归位 ✓ —— `delivery/`（7 份交付报告 ✓）· `requirements/`（2 份给后端的需求 ✓）· `design/`（2 份设计 ✓），与既有 `plans/` · `report/` · `qa/` 并列 ✓；原 `salvage/`（208K 事故 CSS 快照）已按删除政策清理 ✗（内容与 git 版本重合、教训已入 R27 ✓）。
- **新旧对照表在 `.omp/README.md`** ✓ —— `.omp/` 不入库 ⇒ 搬动**没有版本控制安全网** ✗（已列为该 README 规则 6 ✓）。
- **引用同轮更新** ✓：`AGENTS.md` R25 · `ai-workflow/references.md` · `ai-workflow/practices.md` ✓；搬动曾使 `check-knowledge.sh` 报归档断链 ✗ ⇒ 归档里的路径**改写为可解析形式**（不改事实 ✓）⇒ 新规则 7：**移动后必须改写引用路径，不能"保留旧路径作历史"** ✗（闸门会解析 memory-bank 里每一个 `.omp/…` 串 ✓）。

### 落地页 P2 收官 + 目视验收（2026-09-24，v0.51.0）

- **P2「视觉基元与版面节奏」✅ 完成** ✓：唯一挂账项「面板动效」由 2026-09-21/22 的 AuthLayout 交付解除 ✓；**完成记录第五段**已补入计划 ✓（此前汇总行仍写 🚧、引用的第五段并不存在 ✗）。
- **验收 3/3 实测** ✓（1440×900 · spawned Chrome）：① 令牌全有出处 ✓（header 实测 **57px** = `border-box` 56 + 1px 底边 ✓）；② 双模成立 ✓（h1 **64px/510/−1.408px/64px** 两主题逐值相同 ✓ · 15 处文本 14 处 ≥ 4.5 ✓）；③ reduce `getAnimations()` **0** ⟷ 对照组 **5** ✓；**滚动动效不可测** ✗（`scrollHeight == viewport` ⇒ 无自证能力 ✗）。
- **目视验收 5 项 ✅ 已确认（"没啥问题" ✓）** ⇒ **一律维持现状** ✗：大标题大小（手机 64px 排 4 行 ✓ **不改** ✗）· 眉题（4.24/4.42 ✓ **不改** ✗）· 面板手感 · 亮色四项 · 留白 ✓。结论档 `.omp/qa/visual-qa.md` ✓；**防 churn 条款**入 `landing-page/scenarios.md` LS7 ✓。
- **`ghost` 发光已改为读令牌** ✅（2026-09-24，v0.51.1）：4 处写死的 rgba（`rgba(113,112,255,…)` / `rgba(130,143,255,…)` ✗ 与 `--accent`/`--accent-hover` 恰好同值 ✓）换成 `color-mix(in oklab, var(--accent) 25%, transparent)` 系列 ✓ —— **零新令牌 ✓ 不动 DESIGN.md ✗**；真机逐位复测：亮 `rgb(112,112,255) a=0.251` / `rgb(114,114,255) a=0.149`、暗 `rgb(130,142,255) a=0.400` / `rgb(130,145,255) a=0.200`，**与改前完全一致** ✓（模糊半径 8/2 · 12/4 不变 ✓）；Tailwind 确实编出这 4 条（无静默丢弃 ✓）；单测 **1450/1450** ✓。用户动机原话：现在好，但主题一改就会被破坏 ⇒ 属于真缺陷 ✓ 已修 ✓。
- **探针三坑**（已入领域 ✓）：`browser.open({ viewport })` **不生效** ✗ ⇒ 用 `page.setViewport` ✓；Tailwind v4 的 `color` 是 **`oklab(…)`** ✗ ⇒ 必须过 canvas 归一化 ✓（否则读出 **1.05 假象** ✗）；reduce 把 `transition-duration` 变成 **`0.01ms`** ✗ ⇒ 不能拿「≠ `0s`」当非零判据 ✓。

### AuthLayout 展示面板（2026-09-21，v0.48.1 → v0.51.0 ✓ 已完成）

- 三层光各归其主 ✓（掠光属鼠标 · 聚光描边属几何 · 底光属**卡片**且为**场景级单层** ✓）；一个 **rAF 运动学时钟** + **各向同性半径** `hypot(W,H)/2.4` ✓（此前竖直灵敏度 **2.64×** ✗）+ **静止契约**（恒等矩阵 ✓ `forwards→backwards` ✓ `currentRotate*` 改 `ref` + `rafId` 释放 ✓ —— 真因是 `useCardTilt` 曾是**死代码** ✗）。
- **耐久内容全在 [`domains/auth-layout/`](./domains/auth-layout/meta.md)** ✓（本文件不重复 ✗）；相关纪律入 AGENTS **R27/R28** ✓。

> 本文件只记「现在与最近」✓。**跨轮次可复用的判断在 [`domains/`](./domains/README.md)**（R24）：
> `dashboard-visualization`（图表/配色/布局）· `backend-contract`（契约与统计语义）· `achievements`（奖杯语义与几何）·
> `auth-layout`（面板三层光与运动）· `landing-page`（营销面视觉基线）· `ai-workflow`（记忆/版本/提交/验证/资源）。

## Archived History

- `memory-bank/archives/2026-09-24-activeContext-trim.md` — 2026-09-19 → 2026-09-21 的逐轮细节（P1 · 账号删除 · Leaderboard · 知识库治理 · hero 动效 · 用户级技能）；耐久判断已回迁领域 ✓。
- `memory-bank/archives/2026-09-15-dashboard-era-archive.md` — v0.28 – v0.37 Dashboard 面板迭代。
- `memory-bank/archives/2026-09-10-activeContext-archive.md` — v0.16.14 → v0.34.0 完整时间线。
- `memory-bank/archives/2026-08-16-activeContext-archive.md` — v0.16.13 及更早（v0.8.x 起）。
- 归档惯例：维持 200 行上限，溢出即裁剪 ✓。
